package com.ossom.monitoring.model;

/** Alert severity levels, ordered by ascending criticality (ordinal used for comparison). */
public enum Severity {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
